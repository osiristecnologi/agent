{
  "name": "smart-agent-api",
  "version": "2.0.0",
  "description": "Agente local com compreensão de comandos, memória de sessão e geração de código/HTML.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check brain/brain.js && node --check brain/intent.js && node --check brain/planner.js && node --check brain/skills.js && node --check brain/configStore.js && node --check tools/formatter.js && node --check tools/codeTemplates.js && node --check memory/session.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
